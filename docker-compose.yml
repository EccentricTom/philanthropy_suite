services:
  db:
    image: postgres:16
    environment:
      - POSTGRES_PASSWORD=password
  backend:
    build: ./backend
    depends_on:
      - db
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
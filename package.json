{
  "name": "philanthropy-suite",
  "private": true,
  "scripts": {
    "dev-frontend": "npm run dev --prefix frontend",
    "dev-backend": "cd backend && uv run uvicorn app.main:app --reload",
    "test-backend": "cd backend && uv run pytest"
  }
}